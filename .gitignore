# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# python scraper
scraper/.venv/
scraper/__pycache__/
**/__pycache__/
*.pyc
scraper/.env

# data lourde optionnelle (le geojson des quartiers est commité, lui)
scraper/output/

# vercel
.vercel

.vercel

# archive locale Supabase (volumineuse, hors repo)
archive/
ops/logs/
lib/tension.compiled.mjs

tsconfig.tsbuildinfo

# Pitch decks et leurs visuels — documents commerciaux, gardés en LOCAL.
# Le dépôt est public : les versionner ici les publierait, et git n'oublie pas.
lowi_pitch_deck*.html
pitch-assets/

# système d'agents : état d'exécution local, pas du code
agents/ledger.db
agents/ledger.db-wal
agents/ledger.db-shm
agents/logs/
agents/audits/
agents/state/
agents/queue/
ops/logs/
ops/taches-supprimees/
tests-scrap/
verif_tooltip.ts

# widget de bureau : état local et journal, pas du code
ops/widget/etat.json
ops/widget/widget.log
ops/widget/.arret

# marqueur PAR MACHINE : ce poste n'heberge aucun modele local (T1 delegue a Claude)
agents/t1-absent
agents/state/organize/lots/
